{
    "private": true,
    "scripts": {
        "fix": "prettier . --write && eslint --fix; pre-commit run --all-files || pre-commit run --all-files",
        "prepare": "husky"
    },
    "devDependencies": {
        "eslint": "^9.34.0",
        "eslint-config-prettier": "^10.1.8",
        "husky": "^9.1.7",
        "lint-staged": "^16.1.5",
        "prettier": "^3.6.2",
        "typescript-eslint": "^8.41.0"
    },
    "lint-staged": {
        "**/*.{js,jsx,ts,tsx,json,html,css}": [
            "prettier --write",
            "eslint"
        ]
    }
}
