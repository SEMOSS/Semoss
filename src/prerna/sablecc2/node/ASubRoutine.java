/* This file was generated by SableCC (http://www.sablecc.org/). */

package prerna.sablecc2.node;

import java.util.*;
import prerna.sablecc2.analysis.*;

@SuppressWarnings("nls")
public final class ASubRoutine extends PSubRoutine
{
    private final LinkedList<PSubRoutineOptions> _subRoutineOptions_ = new LinkedList<PSubRoutineOptions>();

    public ASubRoutine()
    {
        // Constructor
    }

    public ASubRoutine(
        @SuppressWarnings("hiding") List<?> _subRoutineOptions_)
    {
        // Constructor
        setSubRoutineOptions(_subRoutineOptions_);

    }

    @Override
    public Object clone()
    {
        return new ASubRoutine(
            cloneList(this._subRoutineOptions_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseASubRoutine(this);
    }

    public LinkedList<PSubRoutineOptions> getSubRoutineOptions()
    {
        return this._subRoutineOptions_;
    }

    public void setSubRoutineOptions(List<?> list)
    {
        for(PSubRoutineOptions e : this._subRoutineOptions_)
        {
            e.parent(null);
        }
        this._subRoutineOptions_.clear();

        for(Object obj_e : list)
        {
            PSubRoutineOptions e = (PSubRoutineOptions) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._subRoutineOptions_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._subRoutineOptions_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._subRoutineOptions_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PSubRoutineOptions> i = this._subRoutineOptions_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PSubRoutineOptions) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
