/* This file was generated by SableCC (http://www.sablecc.org/). */

package prerna.sablecc.node;

import java.util.LinkedList;
import java.util.List;
import java.util.ListIterator;

import prerna.sablecc.analysis.Analysis;

@SuppressWarnings("nls")
public final class AConfiguration extends PConfiguration
{
    private final LinkedList<PScript> _script_ = new LinkedList<PScript>();

    public AConfiguration()
    {
        // Constructor
    }

    public AConfiguration(
        @SuppressWarnings("hiding") List<?> _script_)
    {
        // Constructor
        setScript(_script_);

    }

    @Override
    public Object clone()
    {
        return new AConfiguration(
            cloneList(this._script_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAConfiguration(this);
    }

    public LinkedList<PScript> getScript()
    {
        return this._script_;
    }

    public void setScript(List<?> list)
    {
        for(PScript e : this._script_)
        {
            e.parent(null);
        }
        this._script_.clear();

        for(Object obj_e : list)
        {
            PScript e = (PScript) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._script_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._script_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._script_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PScript> i = this._script_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PScript) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
