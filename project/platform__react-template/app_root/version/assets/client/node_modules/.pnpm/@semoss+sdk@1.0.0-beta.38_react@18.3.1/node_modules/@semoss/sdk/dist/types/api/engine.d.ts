/**
 * Upload file(s) to an engine
 * @param engineId
 * @param path
 * @param files
 * @param insightId
 * @returns
 */
export declare const uploadEngine: (engineId: string, path: string, files: File | File[], insightId: string | null) => Promise<{
    response: Response;
    data: {
        fileName: string;
        fileLocation: string;
    }[];
}>;
