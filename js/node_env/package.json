{
  "name": "semoss-node-env",
  "version": "1.0.0",
  "private": true,
  "description": "Curated package environment for the SEMOSS agent Node.js worker. Installed at deploy time with 'npm ci --omit=dev'; agent code resolves require() against this folder only. Prefer pure-JS packages; sharp is the deliberate exception (ships prebuilt binaries per platform, no node-gyp compile) because the pptx skill's icon pipeline needs it.",
  "dependencies": {
    "cheerio": "^1.0.0",
    "dayjs": "^1.11.13",
    "fast-xml-parser": "^4.5.0",
    "lodash": "^4.17.21",
    "mathjs": "^13.2.0",
    "papaparse": "^5.4.1",
    "pptxgenjs": "^4.0.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.4.0",
    "sharp": "^0.34.1",
    "uuid": "^11.0.0",
    "zod": "^3.23.8"
  }
}
